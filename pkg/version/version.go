package version

var (
	// Version is the build version (e.g. tag or branch name).
	Version = "dev"
	// Commit is the git commit SHA.
	Commit = "none"
	// BuildTime is the date/time of the build.
	BuildTime = "unknown"
)
