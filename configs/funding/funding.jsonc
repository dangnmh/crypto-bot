[
  // ── Per-Symbol configs ──
  // Only symbol + marginUSDT required. Everything else inherits from system.jsonc tradingDefaults.
  // Add any field here to override the default for this specific symbol.

  { "symbol": "COS_USDT", "marginUSDT": 3 }
]
